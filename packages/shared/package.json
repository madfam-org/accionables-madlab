{
  "name": "@madlab/shared",
  "version": "0.1.0",
  "description": "Shared API contract (Zod schemas + types) consumed by both client and server",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build step — types-only package consumed via TS source'",
    "lint": "echo 'no lint step'",
    "test": "echo 'no tests yet'",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
