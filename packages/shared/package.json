{
  "name": "@madlab/shared",
  "version": "0.1.0",
  "description": "Shared API contract (Zod schemas + types) consumed by both client and server",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'no lint step'",
    "test": "echo 'no tests yet'",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  },
  "files": [
    "dist"
  ]
}
